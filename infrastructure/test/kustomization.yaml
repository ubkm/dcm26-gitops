apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This file is the FluxCD entrypoint for the "test" environment.
# From here, we pull in all the applications we want to deploy.

resources:
  # Uncomment in Step X
  # - washere
