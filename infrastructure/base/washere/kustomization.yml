apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - k8s/deployment.yml
  - k8s/ingress.yml
  - k8s/pvc.yml
  - k8s/service.yml

configMapGenerator:
  - name: create-indexfile
    files:
      - create-indexfile.sh